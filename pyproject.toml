[project]
name = "forum"
version = "2026.08.30"
description = "Forum pour la famille et amis"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0,<7.0",
    "gunicorn>=26.2.0",
    "psycopg>=3.1",
]
